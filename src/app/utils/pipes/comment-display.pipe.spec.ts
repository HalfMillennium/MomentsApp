import { CommentDisplayPipe } from './comment-display.pipe';

describe('CommentDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new CommentDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
