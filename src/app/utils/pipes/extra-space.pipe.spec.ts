import { ExtraSpacePipe } from './extra-space.pipe';

describe('ExtraSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new ExtraSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
